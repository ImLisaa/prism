plugins {
    id 'org.jetbrains.kotlin.jvm'
}

group = 'net.phantara.prism'
version = '2024.03.31'

repositories {
    mavenCentral()
}

dependencies {
    implementation('com.github.Minestom:Minestom:b58db7d5b0') {
        exclude group: "org.jboss.shrinkwrap.resolver"
        exclude group: "shrinkwrap-resolver-depchain"
    }
    implementation 'org.jboss.shrinkwrap.resolver:shrinkwrap-resolver-impl-maven:3.3.0'
    implementation 'dev.hollowcube:minestom-ce-extensions:1.2.0'
    implementation 'com.google.guava:guava:27.1-jre'
    implementation 'ch.qos.logback:logback-core:1.5.0'
    implementation 'ch.qos.logback:logback-classic:1.5.0'
    implementation project(path: ':prism-api')
}

kotlin {
    jvmToolchain(17)
}

configurations.implementation.setCanBeResolved(true)

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(17))
    }
    compileJava {
        options.encoding = "UTF-8"
    }
}

jar {
    manifest {
        attributes 'Main-Class': 'net.phantara.prism.server.PrismServerBootstrapKt',
                'Multi-Release': true
    }
    archiveBaseName = 'prism-server'
    archiveVersion = ''
    archiveClassifier = ''

    duplicatesStrategy = DuplicatesStrategy.EXCLUDE

    from {
        configurations.implementation.collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }
}